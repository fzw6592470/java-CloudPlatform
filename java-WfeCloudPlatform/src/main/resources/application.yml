spring:
  application:
    name: java-wfe-cloud-platform
  zipkin:
    base-url: http://localhost:9000
  sleuth:
    sampler:
      percentage: 1.0
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    
server:
  port: 8080

feign:
  hystrix:
    enabled: true